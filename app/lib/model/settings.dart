class Settings {
  int teams;
  int roundTime;
  int cardCount;
  int blackCardCount;
  int whiteCardCount;
  Settings(this.teams, this.roundTime, this.cardCount, this.blackCardCount, this.whiteCardCount);
}